# IMPORTANT: This file contains sensitive configuration and secrets.
# It MUST be added to your .gitignore file to prevent committing it to version control.
# Use .env.example as a template for required variables.

# MongoDB Configuration
# Replace placeholders with your actual database credentials and connection details.
# For local MongoDB: mongodb://localhost:27017/fittrack_mvp_dev
# For MongoDB Atlas: mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/fittrack_mvp_dev

# JWT Authentication Configuration
# Replace this placeholder with a strong, cryptographically secure random string (e.g., `openssl rand -base64 32`).
# This secret is used to sign and verify user session tokens. Keep it confidential.
JWT_SECRET=YOUR_STRONG_JWT_SECRET_HERE_REPLACE_ME

# Defines the duration for which JWT tokens remain valid after login.
# Examples: '1h' (1 hour), '7d' (7 days), '30m' (30 minutes)
JWT_EXPIRES_IN=1d

# Frontend-Exposed Variables (Optional)
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# No NEXT_PUBLIC_ variables are required for the current MVP scope.
# Example: NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id_here